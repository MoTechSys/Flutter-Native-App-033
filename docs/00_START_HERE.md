# سِجِل (Sijil) — ابدأ من هنا

> **لأي وكيل/مطوّر جديد:** اقرأ هذا الملف أولاً، ثم `13_NEW_AGENT_BOOTSTRAP.md` (خطوات البيئة + أسلوب العميل)، ثم `07_PROGRESS.md` لتعرف أين توقفنا بالضبط.
> لا تكتب كوداً قبل قراءة `04_LEDGER_RULES.md` — قواعد الحسابات غير قابلة للتفاوض.

## ما هو المشروع؟
تطبيق Android (Flutter) — **دفتر ديون رقمي للمحلات اليمنية** (بقالة، مطعم، صيدلية، جملة).
يحل محل الدفتر الورقي. يعمل **بدون إنترنت 100%**. مصمَّم ليستخدمه **العامل الأمي** (صور + صوت + أوراق نقدية)
و**المالك المتعلّم** (تقارير + مستندات A4 + عمّال + نسخ سحابي).

- **الاسم:** سِجِل — Sijil
- **الحزمة:** `com.sijildebt.ledger`
- **المنصة:** Android أولاً (96.9% من اليمن)، Web للمعاينة فقط
- **البيئة المثبّتة:** Flutter 3.35.4 / Dart 3.9.2 — **لا تُحدَّث**
- **المستودع:** `/home/user/flutter_app`

## خريطة التوثيق (اقرأ بالترتيب)
| الملف | ماذا فيه | متى تقرأه |
|---|---|---|
| `00_START_HERE.md` | هذا الملف — نظرة عامة + خريطة | دائماً أولاً |
| `01_CONTEXT.md` | من العميل، لماذا الفكرة، الفرص، نموذج الربح، القرارات المتخذة مع أسبابها | لفهم "لماذا" |
| `02_RESEARCH.md` | كل ما بحثناه: السوق اليمني، الأمية، المنافسين، تصميم للأمي، تقنيات | قبل اقتراح أي ميزة |
| `03_DESIGN.md` | نظام التصميم: الألوان (مُدقَّقة WCAG)، الخطوط، المكوّنات، الشاشات، قرارات UX + روابط الموك-أب | قبل أي شاشة |
| `04_LEDGER_RULES.md` | **القواعد العشر للحسابات** + مخطط قاعدة البيانات + حالات الاختبار | **قبل أي كود يلمس المال** |
| `05_ARCHITECTURE.md` | هيكلية المستودع، الطبقات، تدفق البيانات، الحزم وأسباب اختيارها، المزامنة | قبل إضافة ملف |
| `06_PLAN.md` | خارطة الطريق بالمراحل + معايير القبول لكل مرحلة | لمعرفة ما بعد |
| `07_PROGRESS.md` | **سجل التقدّم**: ما أُنجز / ما يجري / ما بقي — يُحدَّث كل جلسة | **دائماً ثانياً** |
| `08_ERRORS_AND_LESSONS.md` | الأخطاء التي واجهناها وحلولها + قرارات تراجعنا عنها | عند أي مشكلة |
| `09_DOCUMENTS_SPEC.md` | مواصفات المستندات الرسمية (A4): الأنواع، القوالب، محرر الترويسة، JSON | قبل العمل على PDF |
| `11_AUDIT_PHASE1_2.md` | تدقيق ما بُني مقابل البحث والخطة: ما تحقق، ما سُدّ من فجوات، الدَّين التقني المُعلَن | قبل الحكم على جودة الكود |
| `13_NEW_AGENT_BOOTSTRAP.md` | **للوكيل الجديد**: نص يلصقه العميل، خطوات البيئة المُجرَّبة، استعادة التوقيع، أسلوب العميل، خريطة الكود، تعريف الجلسة المكتملة | **أول جلسة لك — إلزامي** |
| `12_RELEASE_GUIDE.md` | **دليل الإصدار**: بناء APK أندرويد، التوقيع، توليد أكواد التفعيل، اختبار الميدان (D13) | قبل إعطاء التطبيق لأي زبون |
| `../CHANGELOG.md` | **سجل الإصدارات**: كل APK صادر برقمه، روابط التحميل العامة، SHA-256، الـ commit، خطوات الإصدار الثابتة | **قبل أي بناء APK وبعده** |
| `../README.md` | ملخّص تسليم بالعربية (للمالك + أي وكيل جديد) — يكرّر أهم ما هنا باختصار | نظرة سريعة |
| `design/mockups/` | صور التصاميم المعتمدة والمرفوضة | مرجع بصري |
| `design/pdf_poc_*` | إثبات عملي: PDF عربي A4 وُلِّد على هذه البيئة | مرجع PDF |
| `research/` | ملفات خام: تحليل إكسل العميل القديم، نتائج بحث | مرجع |

## قواعد العمل للوكيل
1. **حدّث `07_PROGRESS.md` قبل إنهاء أي جلسة** — ما فعلت، ما بقي، أي شيء مُعلَّق.
2. **أي خطأ واجهته وحللته → `08_ERRORS_AND_LESSONS.md`** فوراً.
3. **أي قرار تصميمي/تقني جديد → أضفه في الملف المناسب مع السبب.** القرارات بدون أسباب تُنسى.
4. `flutter analyze` صفر مشاكل قبل أي commit.
5. اختبارات محرك الحسابات (`test/ledger/`) يجب أن تنجح 100% قبل أي commit يلمس `lib/core/ledger` أو `lib/core/money`.
6. لا `print()`، لا `withOpacity()`، لا `!` بدون تحقق.
7. الأوامر تُنفَّذ من `/home/user/flutter_app` دائماً.

## المنصّة المستهدفة: أندرويد فقط (قرار العميل — جلسة 4)
- **المنتج = APK أندرويد** صغير الحجم. لا iOS، لا ويب، لا سطح مكتب حالياً.
- الويب يُستخدم **للمعاينة أثناء التطوير فقط** (`--dart-define=DEMO=true`) لأن الساندبوكس لا يشغّل محاكي أندرويد. لا تُضف أي ميزة تعتمد على الويب.
- إعدادات الحجم في `android/app/build.gradle.kts`: `minSdk 21`، `resourceConfigurations ar/en`، `isMinifyEnabled` + `isShrinkResources`، `proguard-rules.pro`، تقسيم حسب المعالج (arm64-v8a + armeabi-v7a، بلا APK شامل).
- النتيجة الحالية (v0.2.0+2): **arm64 ≈ 23.2 MB / armv7 ≈ 21.3 MB** موقّعان.
- ملفات التوقيع `android/key.properties` + `android/release-key.jks` **خارج Git** (.gitignore). إن ضاعت → لا يمكن تحديث التطبيق عند نفس الزبائن دون إعادة تثبيت. انظر `12_RELEASE_GUIDE.md`.

## أوامر سريعة
```bash
cd /home/user/flutter_app
flutter pub get
flutter analyze                       # يجب: No issues found
flutter test                          # كل الاختبارات (93 حالياً)
flutter test test/ledger              # اختبارات المحاسبة فقط

# ===== المنتج الحقيقي: أندرويد =====
flutter build apk --release --split-per-abi
#   → build/app/outputs/flutter-apk/app-arm64-v8a-release.apk   (الهواتف الحديثة)
#   → build/app/outputs/flutter-apk/app-armeabi-v7a-release.apk (الهواتف القديمة الرخيصة)
# ملاحظة: Flutter قد يطبع بعد النجاح "Gradle build failed to produce an .apk file" — رسالة زائفة
# سببها تقسيم ABI؛ تحقق بوجود الملفين أعلاه.

# ===== معاينة تطوير فقط (ويب) =====
flutter build web --release --dart-define=DEMO=true   # ببيانات تجريبية (بقالة الأمل)

# ===== أدوات =====
dart run tool/gen_activation.dart <deviceId> Y1   # توليد كود تفعيل للبائع (Y1|Y3|LIFE)
DUMP_PDF=1 flutter test test/tool_dump_pdfs_test.dart  # عينات PDF إلى /tmp/pdfout
python3 docs/research/shot_p1b.py     # لقطات المسار الكامل عبر رابط المعاينة (عدّل URL أولاً)
```

## GitHub
المستودع: **https://github.com/MoTechSys/Flutter-Native-App-033** (فرع `main` = سِجِل). كل جلسة تنتهي بـ commit + push، وتحديث `07_PROGRESS.md`.

> ⚠️ **تاريخ المستودع:** كان يحوي مشروعاً سابقاً مختلفاً للعميل ("كتابي" — متجر كتب، v1.1.0). في جلسة 4 حُفظ كاملاً على فرع **`kitabi-archive`** (مع وسمَيه v1.0.0/v1.1.0) وأُعيد توجيه `main` إلى سِجِل. لا تحذف `kitabi-archive` ولا تدمجه. الوسوم `v1.x` تخص كتابي، وسوم سِجِل تبدأ بـ `sijil-v0.2.0`.

## اللغة
التوثيق بالعربية (لغة العميل). أسماء الملفات والكود والتعليقات التقنية بالإنجليزية. النصوص الظاهرة للمستخدم عربية.
